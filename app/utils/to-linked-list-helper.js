export default function toLinkedListHelper(nodes, edges) {
  console.log("thaks for calling to-linked-list-helper.js", nodes, edges)
  // _.each(nodes, function(node){
  //   edges_to_parents = _.filter(edges,function(edge){
  //     edges.to === node.id
  //   })
  //   parent_ids = _.map(edges_to_parents,function(edge){
  //     edge.from
  //   })
  // })
}

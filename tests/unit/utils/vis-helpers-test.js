import visHelpers from '../../../utils/vis-helpers';
import { module, test } from 'qunit';

module('visHelpers');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = visHelpers();
  assert.ok(result);
});

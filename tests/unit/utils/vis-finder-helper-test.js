import visFinderHelper from '../../../utils/vis-finder-helper';
import { module, test } from 'qunit';

module('visFinderHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = visFinderHelper();
  assert.ok(result);
});

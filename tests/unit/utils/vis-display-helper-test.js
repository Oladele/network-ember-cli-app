import visDisplayHelper from '../../../utils/vis-display-helper';
import { module, test } from 'qunit';

module('visDisplayHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = visDisplayHelper();
  assert.ok(result);
});

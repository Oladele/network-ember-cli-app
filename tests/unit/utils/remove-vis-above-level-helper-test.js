import removeVisAboveLevelHelper from '../../../utils/remove-vis-above-level-helper';
import { module, test } from 'qunit';

module('removeVisAboveLevelHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = removeVisAboveLevelHelper();
  assert.ok(result);
});
